# TicketWAVES frontend

Upload the contents of this folder to the existing TicketWAVES GitHub Pages frontend. Keep `config.js` pointed at the current Render API.

The UI is mobile-first and keeps Discover, For You, My Tickets, Sell, Account and Admin.
